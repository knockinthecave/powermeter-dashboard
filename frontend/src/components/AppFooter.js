import React from 'react'
import { CFooter } from '@coreui/react-pro'

const AppFooter = () => {
  return <CFooter className="px-4">{/* Empty Footer */}</CFooter>
}

export default React.memo(AppFooter)
